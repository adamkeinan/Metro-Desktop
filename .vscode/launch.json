{
    "version": "0.2.0", 
    "configurations": [ {
            "type": "chrome", 
            "request": "launch", 
            "name": "Attach to Chrome", 
            "port": 9222, 
            "webRoot": "${workspaceFolder}", 
         }
    ]
}
